#include "BibSistema.h" // For GLfloat type, and extern declarations

// Define the global variables that are declared as extern in BibSistema.h
GLfloat tamanhoDoMundo = 100.0f;
GLfloat coordenadasMundo[4] = {-50.0f, 50.0f, -50.0f, 50.0f}; // Xmin, Xmax, Ymin, Ymax
GLfloat naveJogadorTamanho = 5.0f;
GLfloat invaderTamanho = 4.0f; // Size of the invaders (width and height for a square)
// Potentially other globals from BibSistema.h if they exist and need definition
// GLfloat velocidadeJogador = 0.5f; (If this was in BibSistema.h and needs definition)
// GLfloat velocidadeProjetil = 1.0f; (If this was in BibSistema.h and needs definition)
// etc.
